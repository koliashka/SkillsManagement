services:
  back:
    build: .
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_URLS: 'http://0.0.0.0:8080'
  db:
    container_name: post
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres
    ports:
    - 5432:5432
